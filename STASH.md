# External stash

Non-essential files live outside the repo:

**`/home/seanb/Documents/December-2023-stash/`**

See [`MANIFEST.md`](../December-2023-stash/MANIFEST.md) for paths and restore commands.

Regenerate frontend build cache after clone:

```bash
cd frontend-next && npm run build:hub
```
